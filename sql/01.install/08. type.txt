CREATE TYPE public.mdouble AS
(
	id oid,
	doubles double precision[],
	timeline timestamp without time zone[],
	interpolation json
);


CREATE TYPE point AS
(
	pointx real,
	pointy real
);

CREATE TYPE mpoint AS
(
	id oid,
	coordinates point[],
	timeline timestamp without time zone[],
	interpolation json
);